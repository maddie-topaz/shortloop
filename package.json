{
  "name": "shortloop",
  "private": true,
  "workspaces": [
    "packages/*",
    "backend",
    "frontend",
    "infra",
    "infra/policies",
    "e2e"
  ],
  "scripts": {
    "dev": "./scripts/dev.sh",
    "dev:down": "docker compose down",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "test:backend": "npm run test --workspace=backend",
    "test:frontend": "npm run test --workspace=frontend",
    "build": "npm run build --workspace=@shortloop/contract && npm run build --workspace=backend && npm run build --workspace=frontend",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit -p packages/contract/tsconfig.json && tsc --noEmit -p backend/tsconfig.json && tsc --noEmit -p frontend/tsconfig.json && tsc --noEmit -p infra/tsconfig.json && tsc --noEmit -p infra/policies/tsconfig.json && tsc --noEmit -p e2e/tsconfig.json",
    "test:coverage": "npm run test --workspace=backend -- --coverage",
    "test:smoke": "npm run test --workspace=e2e",
    "test:mutation": "npm run test:mutation --workspace=backend"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "concurrently": "^9.0.1",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-jest": "^29.16.6",
    "eslint-plugin-playwright": "^2.11.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "prettier": "^3.9.6",
    "typescript-eslint": "^8.70.0"
  },
  "engines": {
    "node": ">=22"
  }
}
