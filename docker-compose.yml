services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: shortloop
    ports:
      - '5432:5432'
    volumes:
      - shortloop-pg-data:/var/lib/postgresql/data

volumes:
  shortloop-pg-data:
